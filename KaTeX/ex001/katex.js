// npm install katex
// npm install -g katex

import katex from 'katex/dist/katex.mjs'

//euler integral 


const katex = require('katex');

\Gamma(n) = (n-1)!\quad\forall n\in\

\Gamma(z) = \int_0^\infty t^{z-1}e^{-t}dt\
