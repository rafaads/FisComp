#include <math.h>
#include <stdio.h>

#define ORDEM 5

main(){
  double x, p0,p1,p;
  int n;
  
  for(x=-1;x<=1;x+=.001){

    p0=1;
    p1=x;
    for(n=1;n<ORDEM;n++){
      p = ((2.*n+1.)*x*p1-n*p0)/(n+1.);
      p0=p1;
      p1=p;
    }

    printf("%f %f\n",x,p);
  }

}
